import {Request,Response} from "express";
interface methods{
      // post(x:Request,y:Response);
      // get(x:Request,y:Response);
      // delete(x:Request,y:Response);
      // saveDeviceData(x:Request,y:Response);
      receiveLastSendData(x:Request,y:Response);
}
export default methods;